class Swiper {
    constructor(swiper, options = {}) {

        // Set Wwiper target and options object
        this.swiper = document.querySelector(swiper);
        this.options = options;

        // Kick things off
        this.init();
    }

    init() {

    }
}